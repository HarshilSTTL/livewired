---
title: LiveWired — Update Log
date: 2026-09-14
author: Harshil Kachhadiya
tags:
  - fix
  - profiles
  - schema
---

# 📋 LiveWired — Update Log · 2026-09-14

> One file per day. Newest entries at the top.
> Format: `[YYYY-MM-DD HH:MM] | Type | Title`
> Types: `SP` · `FIX` · `TABLE` · `SCHEMA` · `DOCS` · `FEAT`

---

### [2026-09-14 16:20] | SCHEMA + DOCS | Raised max collaborators per event from 5 to 9

**What changed:**
Product asked to raise the accepted-collaborator cap on collaborative events
from 5 to 9. The limit is enforced in two places: the config-driven check in
`create_event` (reads `max_collaborators_per_event` from `system_config`,
default `'5'`) and hardcoded `>= 5` checks in `update_event` (all six inline
occurrences plus `update_event_v2`'s bundled-invite path), `invite_collaborator`
(deprecated but kept for reference), and `respond_collaborator_invite`'s
race-condition re-check.

**Fixed:**
- `system_config.max_collaborators_per_event` default changed `5` → `9`
  (seed data in `init_system_config`, `get_config` example, schema doc default).
- `update_event` — all seven inline `IF v_collab_count >= 5 THEN` caps raised to `9`.
- `invite_collaborator` and `respond_collaborator_invite` — cap checks and
  error messages raised to 9.
- Docs updated to describe the new max-9 limit (API references, schema/table
  docs, `TASKS.md`).

**Files:**
- [[schema/tables/16_system_config.md]] — default value updated
- [[functions/admin/init_system_config.md]] — seed value updated
- [[functions/admin/get_config.md]] — example fallback updated
- [[functions/events/create_event.md]] — config fallback default updated (3 call sites)
- [[functions/events/update_event.md]] — hardcoded cap raised (7 call sites)
- [[functions/events/invite_collaborator.md]] — cap + message updated
- [[functions/events/respond_collaborator_invite.md]] — cap updated
- [[docs/api/events/create_event.md]], [[docs/api/events/update_event.md]], [[docs/api/events/invite_collaborator.md]], [[docs/api/events/respond_collaborator_invite.md]] — parameter/error docs updated
- [[schema/tables/08_event_mst.md]], [[docs/database/tables/08_event_mst.md]], [[docs/database/tables/15_event_collaborators.md]] — business-rule text updated
- [[TASKS.md]] — task description updated

**Still needed (not done in this session):**
- Update the live `system_config` row in Supabase:
  ```sql
  UPDATE system_config SET config_value = '9' WHERE config_key = 'max_collaborators_per_event';
  ```
- Redeploy `update_event` / `update_event_v2`, `invite_collaborator`, and
  `respond_collaborator_invite` SQL functions with the raised hardcoded caps.

---

### [2026-09-14 15:40] | FIX | `get_following_list` showed non-streaming icons next to avatar — QC on Following page

**What changed:**
QC flagged that the Following page shows extra icons next to a creator's
avatar (beyond streaming platform logos), unlike the Creator Search page
which only shows YouTube/Twitch/Kick/Rumble icons there. Root cause:
`get_following_list`'s `platforms` field returned *all* linked platforms
(including additional links like Patreon/Discord), unfiltered — while
`search_profiles_v2`/`v2_1` already filter to main streaming platform IDs
(1-4) for the same avatar-adjacent icon row.

**Fixed:**
- `get_following_list` — `platforms` subquery now filters to
  `p.plat_id IN (1, 2, 3, 4)`, matching `search_profiles_v2` so the
  Following page and Creator Search page render the same icon set.

**Files:**
- [[functions/follow/get_following_list.md]] — added platform ID filter to SQL
- [[docs/api/follow/get_following_list.md]] — documented the filtered behavior

**Still needed (not done in this session):**
- Run `CREATE OR REPLACE FUNCTION get_following_list(...)` (updated SQL) in
  the Supabase SQL editor to deploy the change.
- Verify the Following page in the app now shows only streaming icons.

---

### [2026-09-14] | FIX | Deleted profile names stayed reserved forever — `create_profile_v2` / `update_profile_v2`

**What changed:**
Client reported: deleting a profile (e.g. "Harshil") and then trying to create
a new profile with the same name failed with `"Profile name already taken"`,
even though the old profile was gone from the user's point of view. Root
cause was two layers deep:

1. `creator_profiles.profile_name` had a full `UNIQUE` constraint at the DB
   level — soft-deleting a profile (`status = 'deleted'`) never freed the name.
2. `create_profile` and `update_profile`'s own uniqueness checks matched
   against *all* profiles regardless of `status`, so even after fixing (1)
   they'd still reject the reuse.

**Fixed:**
- `creator_profiles` — dropped the table-level `UNIQUE (profile_name)`
  constraint, replaced with a partial unique index
  `unique_profile_name_active` scoped to `WHERE status != 'deleted'`.
- `create_profile_v2` — uniqueness check now excludes `status = 'deleted'`
  rows. New endpoint `POST /rpc/create_profile_v2`; `v1` kept deprecated for
  backwards compatibility.
- `update_profile_v2` — same fix for the rename-uniqueness check. New
  endpoint `POST /rpc/update_profile_v2`; `v1` kept deprecated.

**Files:**
- [[schema/tables/05_creator_profiles.md]] — dropped UNIQUE constraint, added partial unique index + migration SQL
- [[functions/profiles/create_profile.md]] — added `create_profile_v2`, kept `v1` deprecated
- [[functions/profiles/update_profile.md]] — added `update_profile_v2`, kept `v1` deprecated
- [[docs/api/profiles/create_profile.md]] — endpoint + error-case note updated
- [[docs/api/profiles/update_profile.md]] — endpoint + error-case note updated
- [[docs/database/tables/05_creator_profiles.md]] — business rule updated to describe partial-index uniqueness

**Still needed (not done in this session):**
- Run the migration in Supabase SQL editor:
  ```sql
  ALTER TABLE public.creator_profiles DROP CONSTRAINT IF EXISTS unique_profile_name;
  CREATE UNIQUE INDEX IF NOT EXISTS unique_profile_name_active
      ON public.creator_profiles (profile_name)
      WHERE status != 'deleted';
  ```
- Deploy `create_profile_v2` and `update_profile_v2` functions in Supabase.
- Update mobile/web clients to call `create_profile_v2` / `update_profile_v2`
  instead of the deprecated `v1` endpoints.
- `NOTIFY pgrst, 'reload schema';` after deploying.
