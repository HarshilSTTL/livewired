---
title: LiveWired — Update Log
date: 2026-05-28
author: Harshil Kachhadiya
tags:
  - search
  - notifications
  - fix
  - enhancement
---

# 📋 LiveWired — Update Log · 2026-05-28

> One file per day. Newest entries at the top.
> Format: `[YYYY-MM-DD HH:MM] | Type | Title`
> Types: `SP` · `FIX` · `TABLE` · `SCHEMA` · `DOCS`

---

## 2026-05-28

---

### [2026-05-28 18:45] | SCHEMA | Implement drag-drop link reordering (Phase 1: Core APIs) #reordering #platforms #custom-links

**Enhancement:** Created migration, reorder endpoint, and v2.1 profile fetching APIs for drag-drop reordering of social links. #drag-drop #api #versioning #schema

**Purpose:** Allow creators to customize the order platforms, additional links, and custom links appear on their profile.

**Phase 1 Implementation:**

1. **Database Migration**
   - Created `profile_link_preferences` table
   - Stores drag-drop order for 3 link groups: platforms (int[]), additional_ids (int[]), custom_ids (uuid[])
   - Single row per profile with CASCADE DELETE on profile deletion
   - Index on (profile_id) for fast lookups
   - File: [[schema/migrations/create_profile_link_preferences.md]]

2. **Reorder API (Save)**
   - Endpoint: `POST /rpc/reorder_social_links_v2`
   - Saves user's drag-drop reordering to profile_link_preferences table
   - UPSERT pattern: inserts new row if no preferences exist, updates existing
   - Parameters: p_profile_id, p_platform_ids[], p_additional_ids[], p_custom_ids[]
   - Returns: saved order in response data
   - Idempotent: calling twice with same data = same result
   - Partial updates: can save only platforms, skip additional/custom with empty arrays
   - File: [[functions/profiles/reorder_social_links.md]]
   - Doc: [[docs/api/profiles/reorder_social_links.md]]

3. **Profile Fetching API (Read)**
   - Created: `get_profile_by_id_v2_1` endpoint
   - Uses profile_link_preferences table for custom ordering
   - Returns links in order: platforms (reordered) → additional links (reordered) → custom links (reordered)
   - Maintains backward compatibility with v2 response structure
   - Falls back to unordered if no preferences exist
   - File: [[functions/profiles/get_profile_by_id.md]]
   - Doc: [[docs/api/profiles/get_profile_by_id.md]]

**Database Schema:**
```
profile_link_preferences
  ├── id (uuid, PK)
  ├── profile_id (uuid, FK, UNIQUE)
  ├── platform_ids_order (int[] — e.g., [3, 4, 2, 1])
  ├── additional_ids_order (int[] — e.g., [7, 5, 6, 8])
  └── custom_ids_order (uuid[] — e.g., [uuid-10, uuid-8, uuid-9])
```

**Link Group Classification:**
- **Platforms:** IDs 1-4 from platforms table (YouTube, Twitch, Kick, Rumble)
- **Additional Links:** IDs 5-42+ from platforms table
- **Custom Links:** Creator-defined links from profile_custom_links table

**API Request/Response Example (reorder_social_links_v2):**
```json
Request: {
  "p_profile_id": "abc-123-def-456",
  "p_platform_ids": [3, 4, 2, 1],
  "p_additional_ids": [7, 5, 6],
  "p_custom_ids": ["uuid-10", "uuid-8"]
}

Response: {
  "status": true,
  "message": "Links reordered successfully",
  "data": {
    "profile_id": "abc-123-def-456",
    "platform_ids_order": [3, 4, 2, 1],
    "additional_ids_order": [7, 5, 6],
    "custom_ids_order": ["uuid-10", "uuid-8"]
  }
}
```

**Files Created/Modified:**
1. [[schema/migrations/create_profile_link_preferences.md]] — NEW (migration + rollback SQL)
2. [[functions/profiles/reorder_social_links.md]] — NEW (save reorder function)
3. [[docs/api/profiles/reorder_social_links.md]] — NEW (API documentation)
4. [[functions/profiles/get_profile_by_id.md]] — MODIFIED (added v2.1 with preference ordering)
5. [[docs/api/profiles/get_profile_by_id.md]] — MODIFIED (added v2.1 documentation)

**Benefits:**
- ✅ Creators can customize link order via drag-drop UI
- ✅ Order persists across page views and devices
- ✅ Supports all 3 link types (platforms, additional, custom)
- ✅ Graceful fallback: if no preferences set, returns default order
- ✅ UPSERT pattern efficient: single database row per profile
- ✅ Backward compatible: v1 and v2 endpoints still work
- ✅ Migration ready: schema can be deployed to production

**Next Steps:**
- Execute migration on production database
- Create v2.1 versions of remaining GET APIs (get_profile_by_userid_v2_1, search_profiles_v2_1, etc.)
- Implement UI drag-drop component
- Add tests for reorder logic

---

### [2026-05-28 16:30] | FIX | `get_profile_by_id_v2`, `get_profile_by_userid_v2`, `get_all_platforms_v2` — Order platforms by ID on profile detail pages #platforms #ordering

**Enhancement:** Created v2 versions of profile detail APIs that return platforms in consistent order (1→2→3→4: YouTube, Twitch, Kick, Rumble). #api #versioning #fix

**Reason:** Profile detail pages should always display platforms in the same order for consistency and predictability.

**APIs Versioned:**
1. [[get_profile_by_id_v2]] — Platforms ordered by plat_id ASC
2. [[get_profile_by_userid_v2]] — Platforms ordered by plat_id ASC
3. [[get_all_platforms_v2]] — Global platforms ordered by plat_id ASC

**Files Modified:**
1. [[functions/profiles/get_profile_by_id.md]] — Added v2 with platform ordering + v1 preserved
2. [[functions/profiles/get_profile_by_userid.md]] — Added v2 with platform ordering + v1 preserved
3. [[functions/platforms/get_all_platforms.md]] — Added v2 with platform ordering + v1 preserved

**Platform Order (Fixed):**
- ID 1: YouTube
- ID 2: Twitch
- ID 3: Kick
- ID 4: Rumble

**SQL Change:**
- Added `ORDER BY p.plat_id ASC` to json_agg() in platforms subquery

**Benefits:**
- ✅ Consistent platform display across all profile pages
- ✅ Users always see platforms in expected order
- ✅ Backwards compatibility maintained (v1 preserved)
- ✅ Applies to all profile detail page APIs

---

### [2026-05-28 16:15] | SCHEMA | Implement API versioning across all updated functions #versioning

**Enhancement:** Created v2 versions of all modified APIs with backwards-compatible v1 functions preserved. Each new version documents changes clearly. #api #versioning

**APIs Versioned:**
1. [[search_profiles_v2]] — Platform filtering (YouTube, Twitch, Kick, Rumble only)
2. [[search_collaborator_profiles_v2]] — Same platform filtering for collaborator picker
3. [[get_notifications]] — Now v2 with no 2-day limit (v1 kept for reference)
4. [[get_unread_notification_count]] — Now v2 synced with get_notifications
5. [[get_profiles_v2]] — Platform filtering for user search API

**Versioning Structure:**
- V2 functions are endpoints (search_profiles_v2, search_collaborator_profiles_v2, etc.)
- V1 functions preserved in same file for backwards compatibility and reference
- Each v2 has clear documentation of changes made
- No breaking changes to v1 endpoints

**Files Modified:**
1. [[functions/search/search_profiles.md]] — Added v2 with platform filter + v1 preserved
2. [[functions/search/search_collaborator_profiles.md]] — Added v2 with platform filter + v1 preserved
3. [[functions/notifications/get_notifications.md]] — Updated comment with v2 version info
4. [[functions/notifications/get_unread_notification_count.md]] — Updated comment with v2 version info
5. [[functions/profiles/get_profiles.md]] — Added v2 with platform filter + v1 preserved

**Benefits:**
- ✅ Clear change documentation in each file
- ✅ Backwards compatibility maintained (v1 functions still available)
- ✅ New clients can use improved v2 APIs
- ✅ Migration path clear for existing clients
- ✅ Version history preserved in codebase

**Next Steps:**
- API docs should be updated to reference v2 endpoints
- Update existing clients to use v2 endpoints
- Monitor adoption before deprecating v1

---

### [2026-05-28 15:45] | FIX | `get_notifications` — Remove 2-day notification history limit

**Bug Fix:** Fixed unread notification badge showing count despite empty notification list. #notifications #bug-fix

**Root Cause:** Inconsistency between two APIs:
- [[get_unread_notification_count]] — counted ALL unread notifications (no time limit)
- [[get_notifications]] — showed ONLY notifications from past 2 days

**Example Scenario:**
1. User has 5 old unread notifications (older than 2 days)
2. Badge shows "5 unread" (from [[get_unread_notification_count]])
3. Notification page is empty (from [[get_notifications]] filtering by 2-day window)

**Fix Applied:**
- Removed `AND created_at >= NOW() - INTERVAL '2 days'` from [[get_notifications]] query
- Both APIs now consistent: show ALL notifications with no time limit
- Updated function comment and documentation

**Files Modified:**
1. [[functions/notifications/get_notifications.md]] — Removed 2-day time filter in SQL
2. [[docs/api/notifications/get_notifications.md]] — Updated documentation notes

**Related Functions:**
- [[get_unread_notification_count]] — Badge count (filters: is_read=false, is_cleared=false)
- [[mark_notifications_read]] — Mark notifications as read
- [[clear_notifications]] — Hide/clear notifications

**Impact:**
- ✅ Unread badge and notification list are now in sync
- ✅ Users see complete notification history
- ✅ No notifications hidden due to age
- ✅ Better data consistency across APIs

**Commit:** `84757a0` — fix: remove 2-day notification history limit

---

### [2026-05-28 15:30] | FIX | `search_profiles` — Filter platforms to only show main streaming platforms

**Enhancement:** Updated [[search_profiles]] API to exclude custom links and additional links from platform results. #search #platforms #enhancement

**Reason:** Search page should display only main streaming platforms. Custom links and additional links should only appear on the full profile detail page via [[get_profile_custom_links]].

**Change Made:**
- Added filter to platforms query: `AND p.plat_id IN (1, 2, 3, 4)`
- Restricts results to 4 main streaming platforms

**Platform IDs Whitelisted:**
- ID 1: YouTube
- ID 2: Twitch
- ID 3: Kick
- ID 4: Rumble

**Files Modified:**
1. [[functions/search/search_profiles.md]] — Added platform ID filter in SQL query
2. [[docs/api/search/search_profiles.md]] — Updated documentation with platform notes

**Related APIs:**
- [[search_collaborator_profiles]] — Same platform filter applied
- [[get_profile_custom_links]] — Fetch custom links separately

**Impact:**
- ✅ Search results show cleaner platform icons (4 main platforms only)
- ✅ Custom links excluded from search page
- ✅ No breaking changes; cleaner UX for profile discovery
- ✅ Better separation of concerns (search vs. profile detail)

**Commit:** `a434572` — fix: filter search_profiles to show only main platforms

---

## 📊 Summary

**Total Commits:** 2
**Total Files Modified:** 4
**Type Distribution:** 2 FIX (bugs and enhancements)
**Status:** ✅ **ALL PUSHED TO MAIN**

**APIs Affected:**
- [[search_profiles]] ✅
- [[search_collaborator_profiles]] ✅
- [[get_notifications]] ✅
- [[get_unread_notification_count]] ✅

**Related Docs:**
- [[docs/api/search/search_profiles.md]]
- [[docs/api/notifications/get_notifications.md]]
- [[schema/tables/03_platforms.md]]
- [[schema/tables/19_notifications.md]]
