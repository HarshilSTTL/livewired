# LiveWired — Update Log · 2026-05-08

> One file per day. Newest entries at the top.
> Format: `[YYYY-MM-DD] | Type | Title`
> Types: `SP` · `FIX` · `TABLE` · `SCHEMA` · `DOCS`

---

## 2026-05-08

---

### [2026-05-08] | SP + DOCS | Notification read state — tasks #2 · #3 · #4

**Files changed:**
- [[functions/notifications/get_notifications.md]] — added `is_read` to SELECT
- [[functions/notifications/get_unread_notification_count.md]] — new SP
- [[functions/notifications/mark_notifications_read.md]] — new SP
- [[docs/api/notifications/get_notifications.md]] — added `is_read` to response + related links
- [[docs/api/notifications/get_unread_notification_count.md]] — new
- [[docs/api/notifications/mark_notifications_read.md]] — new

**Schema migration required (run once in Supabase SQL editor):**
```sql
ALTER TABLE public.notifications
  ADD COLUMN IF NOT EXISTS is_read boolean NOT NULL DEFAULT false;
```

**Summary:**
- `get_notifications` now returns `is_read` on every notification row
- `get_unread_notification_count` — returns `{ "unread_count": N }` for badge display
- `mark_notifications_read` — marks all or specific notifications as read; owner-scoped

---

### [2026-05-08] | SP + DOCS | `update_event` — recurring scope (`'this'` / `'all'`)

**Files changed:**
- [[functions/events/update_event.md]] — full rewrite with `p_scope` parameter
- [[functions/events/get_profile_events.md]] — platform EXISTS CASE fix
- [[functions/events/get_event_by_id.md]] — platform EXISTS CASE fix
- [[functions/events/get_event_list.md]] — platform EXISTS CASE fix (4 occurrences)
- [[docs/api/events/update_event.md]] — added `p_scope` param, error cases, logic flow update

**Summary:**
- New `p_scope text DEFAULT 'all'` parameter on `update_event`
- `'all'` (default) — updates parent + propagates scalar changes to all child occurrences; clears per-child platform overrides when platforms are replaced
- `'this'` — updates only the specific child occurrence passed; stores platforms directly on the child's own `event_id` as a per-occurrence override
- Read SPs now use `EXISTS CASE` for platform resolution — child's own `event_platforms` rows take precedence over parent's
- Recurring schedule and collaborator changes are blocked for `p_scope = 'this'`
