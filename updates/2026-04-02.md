# LiveWired — Updates: 2026-04-02

---

### [2026-04-02 10:00] | FIX | get_profile_events — reverted input back to p_profile_id + p_week_start

**Problem:** Previous session changed input to `p_username text` but the correct caller contract is `p_profile_id uuid` + `p_week_start date`.

**Fix:** Reverted SP and docs to use `p_profile_id uuid` as the primary filter. `p_week_start date` unchanged.

**Files changed:**
- `functions/events/get_profile_events.md` — reverted to p_profile_id uuid input
- `docs/api/events/get_profile_events.md` — reverted params, request example, logic flow

---

### [2026-04-02 10:15] | FIX | follow_creator — removed p_device_ip dead code

**Problem:** Runtime error — `column "p_device_ip" does not exist`. The SP body referenced `p_device_ip` in an UPDATE block but it was never declared as a function parameter.

**Fix:** Removed the 6-line dead block that attempted to update `users.updated_device_ip`:
```sql
-- REMOVED:
IF p_device_ip IS NOT NULL THEN
  UPDATE public.users SET updated_device_ip = p_device_ip, updated_at = now()
  WHERE id = p_user_id;
END IF;
```

**Files changed:**
- `functions/follow/follow_creator.md` — dead code block removed

---

### [2026-04-02 10:30] | FIX | register + login — renamed params to p_ prefix (column ambiguity fix)

**Problem:** Runtime error — `column reference "email" is ambiguous`. Parameters named `email` and `password` clashed with same-named columns in the `users` table during SELECT/INSERT.

**Fix:** Renamed all function parameters to use `p_` prefix throughout:
- `email` → `p_email`
- `password` → `p_password`
- `created_device_ip` → `p_created_device_ip`

**Files changed:**
- `functions/auth/register.md` — all params renamed to p_ prefix
- `functions/auth/login.md` — all params renamed to p_ prefix
- `docs/api/auth/register.md` — param table updated
- `docs/api/auth/login.md` — param table updated

---

### [2026-04-02 10:45] | FIX | signup + google_auth — v_user_id type fixed bigint → uuid

**Problem:** Runtime error — `invalid input syntax for type bigint: "uuid"`. Live database uses `users.id uuid DEFAULT gen_random_uuid()` but the SPs declared `v_user_id bigint`, causing type mismatch on `RETURNING id INTO v_user_id`.

**Fix:** Changed `v_user_id` declaration from `bigint` to `uuid` in both SPs.

**Schema correction:** `schema/tables/02_users.md` updated — `id` corrected from `int8 GENERATED ALWAYS AS IDENTITY` to `uuid DEFAULT gen_random_uuid()`.

**Migration note:** No migration needed — live DB already uses uuid. This was a documentation error.

**Files changed:**
- `functions/auth/signup.md` — v_user_id changed to uuid
- `functions/auth/google_auth.md` — v_user_id changed to uuid
- `schema/tables/02_users.md` — id type corrected to uuid

---

### [2026-04-02 11:00] | FIX | google_auth — password NOT NULL constraint (migration required)

**Problem:** Runtime error — `null value in column "password" violates not-null constraint`. Live DB had `NOT NULL` on the `password` column, but Google users need `password = NULL`.

**Migration required (run once in Supabase SQL editor):**
```sql
ALTER TABLE public.users ALTER COLUMN password DROP NOT NULL;
```

**Files changed:**
- `schema/tables/02_users.md` — password column marked as nullable, migration note added
- `docs/database/tables/02_users.md` — password nullable note updated

---

### [2026-04-02 14:00] | SP | get_profiles — added fuzzy match on profile_name + username, trimmed response fields

**Changes:**
- Search now uses `word_similarity > 0.3` (pg_trgm) in addition to ILIKE — catches typos and partial matches
- Search scope: `profile_name` and `username` only (not bio)
- When keyword present: results ordered by best fuzzy score DESC, then `created_at DESC`
- When no keyword: ordered by `created_at DESC` (unchanged)
- Response fields trimmed to: `profile_id`, `profile_name`, `avatar`, `followers`, `platforms`
- Removed: `user_id`, `username`, `bio`, `status`, `show_followers`, `tags`, `created_at`
- `followers` always returned as live count (no `show_followers` restriction)

**Files changed:**
- `functions/profiles/get_profiles.md` — fuzzy match added, fields trimmed
- `docs/api/profiles/get_profiles.md` — behaviour notes + diff table updated

---

### [2026-04-02 13:00] | SP | get_profiles — dashboard profile browser with optional search + pagination

**New SP:** `get_profiles` (POST /rpc/get_profiles)
- All params optional — no keyword required
- No keyword / empty input → returns ALL active profiles, paginated
- Keyword provided → filters by `profile_name` and `username` (ILIKE, case-insensitive)
- Pagination via `p_limit` (default 20, max 100) + `p_offset` (default 0)
- Response includes `total` count for UI pagination logic

**Kept separate from `search_profiles`** — old SP unchanged, still used for dedicated search with fuzzy matching.

| | `get_profiles` | `search_profiles` |
|---|---|---|
| Keyword required | No | Yes (min 2 chars) |
| Pagination | Yes (`total`/`limit`/`offset`) | No |
| Fuzzy match | No — ILIKE only | Yes — `word_similarity` |
| Use case | Dashboard browse + search | Search screen |

**Files created:**
- `functions/profiles/get_profiles.md` — SP SQL
- `docs/api/profiles/get_profiles.md` — full API docs

---

### [2026-04-02 12:00] | DOCS | updates/log.md restructured into separate daily files

Extracted all log entries from `updates/log.md` into per-day files:
- `updates/2026-03-30.md` — 9 entries (INIT through ALL 12 TABLES COMPLETE)
- `updates/2026-03-31.md` — 12 entries (update_profile through API_INDEX)
- `updates/2026-04-01.md` — 6 entries (recurring fix through create_profile role gate)
- `updates/2026-04-02.md` — this file

`updates/log.md` rewritten as an index linking to each daily file.

**Files changed:**
- `updates/log.md` — rewritten as daily index
- `updates/2026-03-30.md` — created
- `updates/2026-03-31.md` — created
- `updates/2026-04-01.md` — created
- `updates/2026-04-02.md` — created (this file)

---
