# LiveWired — Updates: 2026-04-02

> Most recent entries at the top.

---

### [2026-04-02 18:00] | SP | get_event_list — added p_user_id filter (followed profiles only)

**Change:** Added `p_user_id uuid DEFAULT null` parameter.

When `p_user_id` is provided → only events from profiles the user actively follows (`follows.is_active = true`) are returned. Dashboard now shows a personalised feed instead of all events.

When `p_user_id` is null → all events returned (original behaviour, backward compatible).

Filter applied to all 3 date branches: past, today (live + today sections), future.

```sql
AND (
    p_user_id IS NULL
    OR cp.id IN (
        SELECT profile_id FROM follows
        WHERE user_id = p_user_id AND is_active = true
    )
)
```

**Files changed:**
- `functions/events/get_event_list.md` — p_user_id param + follow filter added to all 3 branches
- `docs/api/events/get_event_list.md` — description + params table + request examples updated

---

### [2026-04-02 17:00] | SP | get_suggested_profiles — personalised creator suggestions for onboarding

**New SP:** `get_suggested_profiles` (POST /rpc/get_suggested_profiles)

Used after `submit_platform` + `submit_tags` in onboarding to show creators to follow.

**Scoring:** `match_score = platform_matches + tag_matches`
- Platform match: creator has a platform the user also selected (`user_preferred_platforms`)
- Tag match: creator has a tag the user also selected (`user_interests`)
- Results ordered: match_score DESC → followers DESC

**Exclusions:**
- Already-followed profiles (`follows.is_active = true`)
- User's own profiles (`creator_profiles.user_id = p_user_id`)

**Edge case:** User skipped platform/tag steps → all scores = 0 → ordered by followers DESC

**Files changed:**
- `functions/profiles/get_suggested_profiles.md` — NEW SP SQL
- `docs/api/profiles/get_suggested_profiles.md` — NEW full API docs

---

### [2026-04-02 16:00] | DOCS | App screenshot references added to 6 API docs

Created `docs/assets/screenshots/` folder. Added `## App Screen` section with image path and save instructions to:

| API doc | Screenshot filename | Screen |
|---------|-------------------|--------|
| `docs/api/auth/login.md` | `login.png` | Sign In screen |
| `docs/api/auth/signup.md` | `signup.png` | Create Account screen |
| `docs/api/profiles/get_profile_by_id.md` | `profile_view.png` | Profile header + Links row |
| `docs/api/events/get_profile_events.md` | `profile_events.png` | Weekly calendar + events |
| `docs/api/platforms/get_all_platforms.md` | `onboarding_platforms.png` | Choose Platforms step |
| `docs/api/tags/get_all_tags.md` | `onboarding_interests.png` | What are you into? step |

**Files changed:**
- `docs/api/auth/login.md`
- `docs/api/auth/signup.md`
- `docs/api/profiles/get_profile_by_id.md`
- `docs/api/events/get_profile_events.md`
- `docs/api/platforms/get_all_platforms.md`
- `docs/api/tags/get_all_tags.md`

---

### [2026-04-02 15:00] | SP | get_profiles — platforms trimmed to platform_id + logo_url only

Removed `platform_name` and `channel_url` from platforms subquery.
Dashboard card only needs the icon (logo_url) for display.

**Files changed:**
- `functions/profiles/get_profiles.md` — platforms object updated
- `docs/api/profiles/get_profiles.md` — response example updated

---

### [2026-04-02 14:00] | SP | get_profiles — fuzzy match added + response fields trimmed

**Changes:**
- Search now uses `word_similarity > 0.3` (pg_trgm) in addition to ILIKE — catches typos and partial matches
- Search scope: `profile_name` and `username` only
- When keyword present: results ordered by fuzzy score DESC, then `created_at DESC`
- When no keyword: ordered by `created_at DESC`
- Response fields trimmed to: `profile_id`, `profile_name`, `avatar`, `followers`, `platforms`
- Removed: `user_id`, `username`, `bio`, `status`, `show_followers`, `tags`, `created_at`

**Files changed:**
- `functions/profiles/get_profiles.md` — fuzzy match + ORDER BY updated, fields trimmed
- `docs/api/profiles/get_profiles.md` — behaviour notes + diff table updated

---

### [2026-04-02 13:00] | SP | get_profiles — new dashboard profile browser with optional search + pagination

**New SP:** `get_profiles` (POST /rpc/get_profiles)
- All params optional — no keyword required
- No keyword → returns ALL active profiles, paginated
- Keyword → filters by `profile_name` and `username`
- Pagination via `p_limit` (default 20, max 100) + `p_offset` (default 0)
- Response includes `total` for UI pagination

Kept separate from `search_profiles` — old SP unchanged.

**Files changed:**
- `functions/profiles/get_profiles.md` — NEW SP SQL
- `docs/api/profiles/get_profiles.md` — NEW full API docs

---

### [2026-04-02 12:00] | DOCS | updates/log.md restructured into separate daily files

Extracted all log entries from `updates/log.md` into per-day files.

**Files changed:**
- `updates/log.md` — rewritten as index
- `updates/2026-03-30.md` — created (9 entries)
- `updates/2026-03-31.md` — created (12 entries)
- `updates/2026-04-01.md` — created (6 entries)
- `updates/2026-04-02.md` — created (this file)

---

### [2026-04-02 11:00] | FIX | google_auth — password NOT NULL constraint (migration required)

**Problem:** `null value in column "password" violates not-null constraint`

**Migration required:**
```sql
ALTER TABLE public.users ALTER COLUMN password DROP NOT NULL;
```

**Files changed:**
- `schema/tables/02_users.md` — password marked nullable, migration note added
- `docs/database/tables/02_users.md` — password nullable note updated

---

### [2026-04-02 10:45] | FIX | signup + google_auth — v_user_id type fixed bigint → uuid

**Problem:** `invalid input syntax for type bigint: "uuid"` — live DB uses `users.id uuid` but SPs declared `v_user_id bigint`

**Files changed:**
- `functions/auth/signup.md` — v_user_id changed to uuid
- `functions/auth/google_auth.md` — v_user_id changed to uuid
- `schema/tables/02_users.md` — id type corrected to uuid

---

### [2026-04-02 10:30] | FIX | register + login — params renamed to p_ prefix (column ambiguity)

**Problem:** `column reference "email" is ambiguous` — params named `email`/`password` clashed with column names

**Fix:** All params renamed with `p_` prefix: `email` → `p_email`, `password` → `p_password`, `created_device_ip` → `p_created_device_ip`

**Files changed:**
- `functions/auth/register.md` — params renamed
- `functions/auth/login.md` — params renamed
- `docs/api/auth/register.md` — param table updated
- `docs/api/auth/login.md` — param table updated

---

### [2026-04-02 10:15] | FIX | follow_creator — removed p_device_ip dead code

**Problem:** `column "p_device_ip" does not exist` — referenced in SP body but never declared as a parameter

**Fix:** Removed the 6-line block that attempted to update `users.updated_device_ip`

**Files changed:**
- `functions/follow/follow_creator.md` — dead code block removed

---

### [2026-04-02 10:00] | FIX | get_profile_events — reverted input back to p_profile_id + p_week_start

**Problem:** Previous session incorrectly changed input to `p_username text`

**Fix:** Reverted to `p_profile_id uuid` + `p_week_start date`

**Files changed:**
- `functions/events/get_profile_events.md` — reverted to p_profile_id uuid
- `docs/api/events/get_profile_events.md` — params, request example, logic flow reverted

---
