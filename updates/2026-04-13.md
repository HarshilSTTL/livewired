# LiveWired — Update Log
> One file per day. Newest entries at the top.
> Format: `[YYYY-MM-DD HH:MM] | Type | Title`
> Types: `SP` · `FIX` · `TABLE` · `SCHEMA` · `DOCS`

---

## 2026-04-13

---

### [2026-04-13 04:00] | FIX | add_custom_link — loop body still reading old profile_name/profile_url keys

**Bug:** SP returned `status: true, count: 0` for every request. The loop read `v_item->>'profile_name'` and `v_item->>'profile_url'` from the jsonb input, but all requests were sending `platform_name` / `platform_url`. Every item hit `CONTINUE WHEN v_platform_name IS NULL` and was skipped.

**Fix:** Updated loop body to read `platform_name` and `platform_url` from jsonb, and INSERT into `platform_name` / `platform_url` columns.

**Files updated:**
- [[functions/profiles/add_custom_link.md]]

---

### [2026-04-13 03:00] | SCHEMA + SP | profile_custom_links — rename columns + update_custom_link to array format

**What changed:**
1. Renamed `profile_custom_links` columns: `profile_name` → `platform_name`, `profile_url` → `platform_url`
2. Updated `update_custom_link` SP to accept array format (same shape as `add_custom_link`)

**Migration SQL (run in Supabase SQL Editor):**
```sql
ALTER TABLE public.profile_custom_links RENAME COLUMN profile_name TO platform_name;
ALTER TABLE public.profile_custom_links RENAME COLUMN profile_url  TO platform_url;
```

**New `update_custom_link` request shape:**
```json
{
  "p_profile_id": "profile-uuid",
  "p_user_id":    "user-uuid",
  "p_links": [
    { "id": "row-uuid-1", "platform_name": "My Portfolio", "platform_url": "https://myportfolio.com" },
    { "id": "row-uuid-2", "platform_name": "My Blog",      "platform_url": "https://myblog.com" }
  ]
}
```

**Files updated:**
- [[schema/tables/14_profile_custom_links.md]] — column rename + migration note
- [[docs/database/tables/14_profile_custom_links.md]] — column rename
- [[functions/profiles/update_custom_link.md]] — array format + column rename
- [[docs/api/profiles/update_custom_link.md]] — array format + column rename
- [[functions/profiles/add_custom_link.md]] — column rename in SQL
- [[docs/api/profiles/add_custom_link.md]] — field names updated
- [[functions/platforms/get_profile_custom_links.md]] — response field names
- [[docs/api/platforms/get_profile_custom_links.md]] — response field names
- [[functions/platforms/manage_custom_links.md]] — field names updated
- [[docs/api/platforms/manage_custom_links.md]] — field names updated

---

### [2026-04-13 02:00] | SP | add_profile_platform + add_custom_link — changed to accept arrays

**What changed:**
Both SPs now accept arrays instead of single-item parameters.

`add_profile_platform` — replaced `(p_platform_id bigint, p_channel_url text)` with `p_platforms jsonb` array. Each item upserted (UPDATE if active row exists, INSERT if not).

`add_custom_link` — replaced `(p_profile_name text, p_profile_url text)` with `p_links jsonb` array. Each item always inserts a new row.

**New request shapes:**
```json
// add_profile_platform
{
  "p_profile_id": "uuid", "p_user_id": "uuid",
  "p_platforms": [
    { "platform_id": 1, "channel_url": "https://youtube.com/@handle" }
  ]
}

// add_custom_link
{
  "p_profile_id": "uuid", "p_user_id": "uuid",
  "p_links": [
    { "profile_name": "My Portfolio", "profile_url": "https://myportfolio.com" }
  ]
}
```

**Files updated:**
- [[functions/profiles/add_profile_platform.md]]
- [[docs/api/profiles/add_profile_platform.md]]
- [[functions/profiles/add_custom_link.md]]
- [[docs/api/profiles/add_custom_link.md]]

---

### [2026-04-13 01:00] | SP | Platform & Custom Link CRUD — 7 new stored procedures

**What changed:**
Replaced single `(p_platform_id bigint, p_channel_url text)` parameters with `p_platforms jsonb` array. Now accepts multiple platforms in one request. Each item is upserted — UPDATE if active row exists, INSERT if not. Invalid/missing items silently skipped.

**New request shape:**
```json
{
  "p_profile_id": "uuid",
  "p_user_id":    "uuid",
  "p_platforms": [
    { "platform_id": 1, "channel_url": "https://youtube.com/@handle" },
    { "platform_id": 2, "channel_url": "https://twitch.tv/handle" }
  ]
}
```

**Files updated:**
- [[functions/profiles/add_profile_platform.md]]
- [[docs/api/profiles/add_profile_platform.md]]

---

### [2026-04-13 01:00] | SP | Platform & Custom Link CRUD — 7 new stored procedures

**What changed:**
Built 7 separate CRUD SPs for managing platform links (Section 1 + 2) and custom links (Section 3) on the profile edit screen. Each operation has its own dedicated SP — no bulk replace, no delete-all-and-recreate.

**SPs added:**

| SP | Endpoint | Operation | Table |
|---|---|---|---|
| `get_profile_platforms` | POST /rpc/get_profile_platforms | Read all active platform links | `creator_platform_accounts` |
| `add_profile_platform` | POST /rpc/add_profile_platform | Insert one platform link | `creator_platform_accounts` |
| `update_profile_platform` | POST /rpc/update_profile_platform | Update channel_url by row id | `creator_platform_accounts` |
| `delete_profile_platform` | POST /rpc/delete_profile_platform | Soft-delete by row id | `creator_platform_accounts` |
| `add_custom_link` | POST /rpc/add_custom_link | Insert one custom link | `profile_custom_links` |
| `update_custom_link` | POST /rpc/update_custom_link | Update name + URL by row id | `profile_custom_links` |
| `delete_custom_link` | POST /rpc/delete_custom_link | Soft-delete by row id | `profile_custom_links` |

**Design notes:**
- All write SPs verify ownership via `creator_profiles.user_id`
- All deletes are soft (`is_deleted = true`, `deleted_at = now()`)
- `get_profile_platforms` returns the `creator_platform_accounts.id` needed as `p_id` for update/delete calls
- `get_profile_custom_links` (existing SP) returns the `profile_custom_links.id` needed for custom link update/delete calls

**Files added:**
- [[functions/profiles/get_profile_platforms.md]]
- [[functions/profiles/add_profile_platform.md]]
- [[functions/profiles/update_profile_platform.md]]
- [[functions/profiles/delete_profile_platform.md]]
- [[functions/profiles/add_custom_link.md]]
- [[functions/profiles/update_custom_link.md]]
- [[functions/profiles/delete_custom_link.md]]
- [[docs/api/profiles/get_profile_platforms.md]]
- [[docs/api/profiles/add_profile_platform.md]]
- [[docs/api/profiles/update_profile_platform.md]]
- [[docs/api/profiles/delete_profile_platform.md]]
- [[docs/api/profiles/add_custom_link.md]]
- [[docs/api/profiles/update_custom_link.md]]
- [[docs/api/profiles/delete_custom_link.md]]
- [[docs/API_INDEX.md]] — updated SP count 26 → 33, Profiles group 7 → 14

---

### [2026-04-13 00:00] | FIX | creator_platform_accounts — soft delete + is_deleted filter across all read SPs

**What changed:**
Added `is_deleted` and `deleted_at` columns to `creator_platform_accounts` to support soft delete.
Updated all SPs that read from this table to filter `AND cpa.is_deleted = false`.
Updated `update_profile` to upsert platforms (UPDATE if exists, INSERT if not) instead of DELETE+INSERT.

**Schema change:**
```sql
ALTER TABLE public.creator_platform_accounts
    ADD COLUMN is_deleted boolean NOT NULL DEFAULT false,
    ADD COLUMN deleted_at timestamp NULL;
```

**SPs updated:**

| SP | Change |
|---|---|
| `update_profile` | Platform handling changed from DELETE+INSERT → upsert (UPDATE if exists, INSERT if not) |
| `get_profile_by_id` | Added `AND cpa.is_deleted = false` |
| `get_profile_by_username` | Added `AND cpa.is_deleted = false` |
| `get_profile_by_userid` | Added `AND cpa.is_deleted = false` |
| `get_creators` | Added `AND cpa.is_deleted = false` |
| `get_following_list` | Added `AND cpa.is_deleted = false` |
| `search_profiles` | Added `AND cpa.is_deleted = false` |
| `get_profiles` | Added `AND cpa.is_deleted = false` |
| `get_suggested_profiles` | Added `AND cpa.is_deleted = false` (platforms subquery + match score subquery) |

**Files changed:**
- [[schema/tables/06_creator_platform_accounts.md]] — added is_deleted + deleted_at columns + migration note
- [[docs/database/tables/06_creator_platform_accounts.md]] — updated column table + business rules
- [[functions/profiles/update_profile.md]] — upsert logic replaces DELETE+INSERT
- [[functions/profiles/get_profile_by_id.md]] — is_deleted filter
- [[functions/profiles/get_profile_by_username.md]] — is_deleted filter
- [[functions/profiles/get_profile_by_userid.md]] — is_deleted filter
- [[functions/follow/get_creators.md]] — is_deleted filter
- [[functions/follow/get_following_list.md]] — is_deleted filter
- [[functions/search/search_profiles.md]] — is_deleted filter
- [[functions/profiles/get_profiles.md]] — is_deleted filter
- [[functions/profiles/get_suggested_profiles.md]] — is_deleted filter (2 locations)

---
