# LiveWired — Update Log · 2026-05-07

> One file per day. Newest entries at the top.
> Format: `[YYYY-MM-DD HH:MM] | Type | Title`
> Types: `SP` · `FIX` · `TABLE` · `SCHEMA` · `DOCS`

---

## 2026-05-07

---

### [2026-05-07] | FIX + SP | Collaborator permission correction + create_event bundled invites + search_collaborator_profiles

**Files changed:**
- [[functions/events/update_event.md]] — owner-only ownership check (collaborator branch removed)
- [[functions/events/delete_event.md]] — owner-only ownership check (collaborator branch removed)
- [[functions/events/create_event.md]] — added `p_collaborator_ids`, collaborator validation, invite loop
- [[functions/search/search_collaborator_profiles.md]] — new SP
- [[docs/api/events/create_event.md]]
- [[docs/api/events/update_event.md]]
- [[docs/api/events/delete_event.md]]
- [[docs/api/search/search_collaborator_profiles.md]] — new
- [[docs/database/tables/08_event_mst.md]] — corrected collaborator permissions note
- [[docs/database/tables/15_event_collaborators.md]] — corrected permissions + Referenced By table
- [[CONTEXT.md]] — SP count 24 → 25

---

**1. FIX — Collaborator permissions corrected in `update_event` and `delete_event`**

- **Before:** Both SPs accepted accepted collaborators as a second valid ownership path, granting them update and delete rights.
- **After:** Both SPs are **owner-only**. The collaborator branch (`NOT EXISTS (... event_collaborators ...)`) has been removed entirely. A collaborator calling either SP receives `"Event not found or access denied"`.
- **Why:** The requirement explicitly states collaborators must not have update, delete, or postpone permissions.

---

**2. SP — `create_event` now accepts `p_collaborator_ids uuid[] DEFAULT null`**

New parameter added after `p_is_collaborative`:

| New param | Type | Default | Notes |
|---|---|---|---|
| `p_collaborator_ids` | `uuid[]` | null | Profile IDs to invite at creation time. Requires `p_is_collaborative = true`. Max 5. |

**Validation (before event insert):**
- If `p_collaborator_ids` is non-empty AND `p_is_collaborative = false` → error: `"Cannot add collaborators when is_collaborative is false"`

**Invite loop (after event insert + recurring generation):**

For each ID in `p_collaborator_ids`:
- Skip if ID = `p_profile_id` (cannot invite self)
- Skip if `v_collab_count >= 5` (hard cap)
- Skip if profile not found or not active
- Otherwise: INSERT into `event_collaborators` (status = `'pending'`) + INSERT notification for invitee

Skipped IDs are collected and returned in `data.skipped_collaborator_ids` (always present, empty array `[]` when nothing was skipped).

**Updated RETURN:**
```json
{
  "status":  true,
  "message": "Event created successfully",
  "data": {
    "event_id":                 "uuid",
    "skipped_collaborator_ids": []
  }
}
```

---

**3. SP — `search_collaborator_profiles` (new, SP #25)**

- **Endpoint:** `POST /rpc/search_collaborator_profiles`
- **Purpose:** Profile search scoped to the collaborator picker. Always excludes the creating/inviting profile. Optionally excludes already-invited profiles for a given event.

**Parameters:**

| Param | Type | Required | Notes |
|---|---|---|---|
| `p_keyword` | text | ✅ | Min 2 chars. Matched against `profile_name` and `bio` |
| `p_exclude_profile_id` | uuid | ✅ | The creating profile — always excluded from results |
| `p_event_id` | uuid | ❌ | If provided, also excludes profiles with pending/accepted active invites for this event |
| `p_limit` | int | ❌ | Default 20 |

**Exclusion logic:**
- `id != p_exclude_profile_id` — always applied
- `id NOT IN (SELECT profile_id FROM event_collaborators WHERE event_id = p_event_id AND status IN ('pending','accepted') AND is_deleted = false)` — applied only when `p_event_id` is provided

---

**4. DOCS — Table docs corrected**

- `event_mst`: collaborator permission note updated — "read-only visibility, cannot update/delete/postpone"
- `event_collaborators`: same correction in Business Rules; Referenced By table updated to reflect owner-only restriction on `update_event` and `delete_event`

---

### [2026-05-07] | DOCS | API doc sync — gaps fixed across event + notification SPs

**Files changed:**
- [[docs/api/events/get_event_by_id.md]]
- [[docs/api/events/get_profile_events.md]]
- [[docs/api/events/get_event_list.md]]
- [[docs/api/notifications/get_notifications.md]]

**Changes:**

| File | What was fixed |
|------|----------------|
| `get_event_by_id` | Added `event_collaborators` to Tables read; added `event_timezone` to response example + Response Field Notes; added collaborators subquery as step 4 in Logic Flow (was missing entirely) |
| `get_profile_events` | Added `event_collaborators` to Tables read; added `is_collaborative` to response example + Response Field Notes; Logic Flow step 4 updated with collaborator OR condition; Profile View Flow output updated |
| `get_event_list` | Added `is_collaborative` to response example (field was returned by function but missing from doc) |
| `get_notifications` | Fixed broken Related link — `../../database/functions/process_event_reminders.md` → `../../../functions/notifications/process_event_reminders.md` |

---

### [2026-05-07] | FIX | `create_event` — recurring child rows missing `is_collaborative`

**Files changed:**
- [[functions/events/create_event.md]]
- [[docs/api/events/create_event.md]]

**Changes:**

**Bug:** Both child-row INSERT blocks (weekly loop and first/last loop) were missing the `is_collaborative` column. Recurring child occurrences were always inserted with `is_collaborative = false` regardless of the parent's setting.

**Fix:** Added `is_collaborative, COALESCE(p_is_collaborative, false)` to both child INSERT column/value lists — matching the parent row INSERT.

**Impact:** Without this fix, collaborative recurring events would never show the collaborative flag on child occurrences in `get_profile_events` or `get_event_list`.

**Also:** Updated `docs/api/events/create_event.md` Tables written header to include `event_collaborators (INSERT if collaborative)` and `notifications (INSERT if collaborative)`.
