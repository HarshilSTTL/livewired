# LiveWired — Update Log · 2026-05-01

> One file per day. Newest entries at the top.
> Format: `[YYYY-MM-DD HH:MM] | Type | Title`
> Types: `SP` · `FIX` · `TABLE` · `SCHEMA` · `DOCS`

---

## 2026-05-01

---

### [2026-05-01 11:00] | SCHEMA | Add event_reminders table and Obsidian vault files

**Obsidian Files Added/Updated:**
- `schema/tables/14_event_reminders.md` (Obsidian Schema Note)
- `docs/assets/screenshots/login.png` (Obsidian Attachment)
- `.obsidian/workspace.json` (Obsidian Workspace Config)
- `.obsidian/bookmarks.json` (Obsidian Bookmarks)
- `.obsidian/.obsidian/` (Nested Obsidian Config files: `app.json`, `appearance.json`, `core-plugins.json`, `workspace.json`)
- `show all files.base` (Obsidian Plugin Configuration)

**Summary:**
All previously untracked Obsidian-related files have been synchronized to the main branch. Environment-specific tool files (e.g., `.claude/`) remain excluded.

### [2026-05-01 00:00] | DOCS | Log update for recent SP changes

**Notes:**
- `get_user_profiles` now returns `p_twitch_by_default` and `p_kick_by_default` per profile (post-login picker).
- Events now support optional `event_end_time` (API param `p_event_end_time`). Live list behavior in `get_event_list` uses start/end time instead of the old 3-hour window.

