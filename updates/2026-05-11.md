# LiveWired — Update Log · 2026-05-11

> One file per day. Newest entries at the top.
> Format: `[YYYY-MM-DD] | Type | Title`
> Types: `SP` · `FIX` · `TABLE` · `SCHEMA` · `DOCS`

---

## 2026-05-11

---

### [2026-05-11] | SP + FIX | `update_event` — auto-null `recurring_interval` for first/last types

- `functions/events/update_event.md` — for `'first'`/`'last'` types, force `v_rec_interval := NULL` (was `COALESCE(passed, stored)` which leaked old weekly interval). Removed the "must be null for first/last" guard.
- `docs/api/events/update_event.md` — synced.

---

### [2026-05-11] | SP + FIX | `update_event` — auto-demote `'this'` for non-recurring / parent rows

- `functions/events/update_event.md` — when `parent_event_id IS NULL`, rewrite `v_scope := 'all'`. Removed the "Scope 'this' can only be used on…" guard.
- `docs/api/events/update_event.md` — synced.

---

### [2026-05-11] | SP + FIX | `update_event` — series-level auto-routing + empty-array tolerance + overload-safe deploy

- `functions/events/update_event.md` — empty arrays equivalent to `null` for `p_recurring_days` / `p_collaborator_ids`. `p_is_collaborative` and `p_collaborator_ids` auto-route to parent for both scopes. `is_overridden` flips true only on real scalar/platform changes. Collaborator append loop moved to a shared section. Scope normaliser handles `null`/`''`. Added drop-all-overloads deploy note.
- `docs/api/events/update_event.md` — synced.

**Deploy:** drop all `update_event` overloads, paste new SP, `NOTIFY pgrst, 'reload schema'`.

---
