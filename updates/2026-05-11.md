# LiveWired — Update Log · 2026-05-11

> One file per day. Newest entries at the top.
> Format: `[YYYY-MM-DD] | Type | Title`
> Types: `SP` · `FIX` · `TABLE` · `SCHEMA` · `DOCS`

---

## 2026-05-11

---

### [2026-05-11] | FEAT + API | `get_notifications` — enhanced with profile context + collaborators

- `functions/notifications/get_notifications.md` — added top-level `profile_id` field. For collaboration notifications (data.type = 'collaborator_invite'), added `collaborators` array with profile info (id, name, avatar) + status for each collaborator.
- `docs/api/notifications/get_notifications.md` — updated response examples for reminder and collaboration notifications. Added collaborators field docs and behavioral notes. Clarified profile_id inclusion.

---

### [2026-05-11] | FEAT + TABLE | Per-follower auto-reminder system (Tasks #12 + #13)

- `schema/tables/10_follows.md` · `schema/tables/16_follow_reminder_dispatches.md` · `functions/follow/update_follow_reminder.md` · `functions/notifications/process_event_reminders.md` · `docs/api/follow/update_follow_reminder.md` — YouTube-style bell icon. Followers opt in per-profile to auto-reminders 1–1440 minutes before each event. Each recurring occurrence is a separate `event_mst` row, so per-follower setting covers all of them. Manual reminders suppress follow-level for same event. Exactly-once delivery via composite PK on dispatches table. Migration: add `reminder_enabled`, `reminder_minutes` to `follows`; create new `follow_reminder_dispatches` table.

---

### [2026-05-11] | SP + FEAT | `skip_recurring_occurrence` — soft-delete single occurrence

- `functions/events/skip_recurring_occurrence.md` · `docs/api/events/skip_recurring_occurrence.md` (NEW) — soft-deletes a child row in a recurring series (sets `is_deleted = true`). Only event owner. Errors on parent or non-recurring.

---

### [2026-05-11] | SP + FEAT | `postpone_recurring_occurrence` — move single occurrence to new date/time

- `functions/events/postpone_recurring_occurrence.md` · `docs/api/events/postpone_recurring_occurrence.md` (NEW) — updates `event_date` + `event_time` on child row only. Validates new_date ≥ today. Only event owner. Errors on parent or non-recurring.

---

### [2026-05-11] | SP + FIX | `update_event` — auto-null `recurring_interval` for first/last types

- `functions/events/update_event.md` — recurring-rule merge now forces `v_rec_interval := NULL` when resolved type is `'first'`/`'last'` (previously `COALESCE` leaked the stored weekly interval). Dropped the "must be null for first/last" guard.
- `docs/api/events/update_event.md` — updated `p_recurring_interval` param description to note auto-null for first/last; removed the "must be null for first/last" error row; updated Logic Flow step 8b.

---

### [2026-05-11] | SP + FIX | `update_event` — auto-demote `'this'` for non-recurring / parent rows

- `functions/events/update_event.md` — added a demotion step after parent resolve: `IF v_scope = 'this' AND v_parent_event_id IS NULL THEN v_scope := 'all'`. Dropped the "Scope 'this' can only be used on…" guard inside Branch A.
- `docs/api/events/update_event.md` — added the "Auto-demotion of 'this'" paragraph in Overview; removed the obsolete error row; new Logic Flow step 6; Behavioural Matrix split rows by event type.

---

### [2026-05-11] | SP + FIX | `update_event` — series-level auto-routing + empty-array tolerance + overload-safe deploy

- `functions/events/update_event.md` — full rewrite with intent flags (`v_update_recurring`, `v_update_collabs`, `v_has_scalar`, `v_has_platforms`). `null` and `[]` now equivalent for `p_recurring_days` / `p_collaborator_ids`. `p_is_collaborative` and `p_collaborator_ids` auto-route to the parent for both scopes (collaborator rejection removed). `is_overridden` flips true only on real scalar/platform changes. Collaborator append loop moved to a shared post-branch section. Added drop-all-overloads deploy note at the top of the SP.
- `docs/api/events/update_event.md` — added Per-scope vs series-level routing table; 13 request examples using test IDs; Behavioural Matrix; rewritten Logic Flow into Branch A / Branch B / Shared sections; Deployment section with the drop+reload SQL; removed obsolete error rows.

**Deploy:** drop all `update_event` overloads, paste new SP, `NOTIFY pgrst, 'reload schema'`.

---
